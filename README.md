# Event Shapes for SUEPs analysis

Initiate an environment: 
```bash
python -m env env.nosync
source env.nosync/bin/activate
pip install coffea jupyter matplotlib mplhep
```

Open the notebook:
```bash
cd CoffeaCode
jupyter notebook
```
and select `ScatterPlot.ipynb`.
